# Q100 系统设计综合题

设计日活千万级 IM 系统的核心在于"消息不丢、不重、有序、低延迟"四大需求：通过 acks=all + 本地消息表确保不丢，雪花 ID + Redis SETNX 去重确保不重，sessionId hash 到同一 Partition 确保有序，Kafka linger.ms=0 + WebSocket 长连接确保低延迟，存储采用 Redis 热缓存 + MySQL 分库分表 + HBase 冷归档三层架构。

## IM 系统全景架构

<svg viewBox="0 0 720 720" xmlns="http://www.w3.org/2000/svg" font-family='-apple-system,"PingFang SC","Microsoft YaHei",sans-serif'>
  <defs>
    <marker id="q100-arrow-blue" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#1565c0"/></marker>
    <marker id="q100-arrow-green" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#3c8772"/></marker>
    <marker id="q100-arrow-red" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#e53935"/></marker>
    <marker id="q100-arrow-orange" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#e65100"/></marker>
    <marker id="q100-arrow-purple" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#7b1fa2"/></marker>
    <marker id="q100-arrow-gray" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#90a4ae"/></marker>
    <filter id="q100-shadow"><feDropShadow dx="1" dy="2" stdDeviation="2" flood-opacity="0.12"/></filter>
    <linearGradient id="q100-grad-blue" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#1565c0"/><stop offset="100%" stop-color="#0d47a1"/></linearGradient>
    <linearGradient id="q100-grad-green" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#3c8772"/><stop offset="100%" stop-color="#2e6b5a"/></linearGradient>
    <linearGradient id="q100-grad-red" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#e53935"/><stop offset="100%" stop-color="#b71c1c"/></linearGradient>
    <linearGradient id="q100-grad-orange" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#e65100"/><stop offset="100%" stop-color="#bf360c"/></linearGradient>
    <linearGradient id="q100-grad-purple" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#7b1fa2"/><stop offset="100%" stop-color="#4a148c"/></linearGradient>
  </defs>

  <!-- 背景 -->
  <rect width="720" height="720" fill="#fafbfc" rx="8"/>

  <!-- ===== 标题 ===== -->
  <rect x="0" y="0" width="720" height="38" rx="8" fill="url(#q100-grad-blue)"/>
  <rect x="0" y="30" width="720" height="8" fill="url(#q100-grad-blue)"/>
  <text x="360" y="26" text-anchor="middle" font-size="16" font-weight="bold" fill="#fff">IM 系统端到端架构 — 日活千万 · 不丢不重有序低延迟</text>

  <!-- ===== 第1层: 用户A 发送消息 ===== -->
  <rect x="8" y="50" width="60" height="38" rx="6" fill="url(#q100-grad-blue)" filter="url(#q100-shadow)"/>
  <text x="38" y="70" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">用户A</text>
  <text x="38" y="84" text-anchor="middle" font-size="8" fill="#fff">发送方</text>

  <line x1="68" y1="69" x2="98" y2="69" stroke="#1565c0" stroke-width="1.5" marker-end="url(#q100-arrow-blue)"/>

  <!-- 第2层: API Gateway -->
  <rect x="100" y="48" width="80" height="40" rx="6" fill="#fff" stroke="#1565c0" stroke-width="1.5" filter="url(#q100-shadow)"/>
  <text x="140" y="65" text-anchor="middle" font-size="11" font-weight="bold" fill="#0d47a1">API Gateway</text>
  <text x="140" y="80" text-anchor="middle" font-size="8" fill="#78909c">鉴权 + 限流</text>

  <line x1="180" y1="69" x2="215" y2="69" stroke="#1565c0" stroke-width="1.5" marker-end="url(#q100-arrow-blue)"/>

  <!-- 第3层: 消息服务 (核心) -->
  <rect x="217" y="44" width="110" height="48" rx="6" fill="url(#q100-grad-blue)" filter="url(#q100-shadow)"/>
  <text x="272" y="62" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Message Service</text>
  <text x="272" y="77" text-anchor="middle" font-size="8" fill="#bbdefb">雪花ID + 本地消息表</text>
  <text x="272" y="90" text-anchor="middle" font-size="8" fill="#bbdefb">acks=all + retries=3</text>

  <!-- 标注: 不丢 ① -->
  <line x1="272" y1="44" x2="272" y2="28" stroke="#3c8772" stroke-width="1" stroke-dasharray="3,2"/>
  <circle cx="272" cy="24" r="7" fill="#3c8772"/>
  <text x="272" y="21" text-anchor="middle" font-size="9" font-weight="bold" fill="#fff">1</text>
  <text x="272" y="16" text-anchor="middle" font-size="7" fill="#3c8772" font-weight="bold">不丢</text>

  <line x1="327" y1="68" x2="357" y2="68" stroke="#1565c0" stroke-width="1.5" marker-end="url(#q100-arrow-blue)"/>

  <!-- 第4层: Kafka 集群 (有序保障) -->
  <rect x="360" y="42" width="195" height="52" rx="6" fill="#fff3e0" stroke="#e65100" stroke-width="1.5" filter="url(#q100-shadow)"/>
  <text x="457" y="57" text-anchor="middle" font-size="11" font-weight="bold" fill="#e65100">Kafka Cluster</text>
  <!-- 3个 Partition -->
  <rect x="370" y="62" width="55" height="26" rx="3" fill="#e65100" opacity="0.12"/>
  <text x="397" y="72" text-anchor="middle" font-size="8" fill="#e65100">Pt-0: sessionId</text>
  <text x="397" y="83" text-anchor="middle" font-size="7" fill="#e65100">hash % 3 = 0</text>

  <rect x="430" y="62" width="55" height="26" rx="3" fill="#e65100" opacity="0.12"/>
  <text x="457" y="72" text-anchor="middle" font-size="8" fill="#e65100">Pt-1: sessionId</text>
  <text x="457" y="83" text-anchor="middle" font-size="7" fill="#e65100">hash % 3 = 1</text>

  <rect x="490" y="62" width="55" height="26" rx="3" fill="#e65100" opacity="0.12"/>
  <text x="517" y="72" text-anchor="middle" font-size="8" fill="#e65100">Pt-2: sessionId</text>
  <text x="517" y="83" text-anchor="middle" font-size="7" fill="#e65100">hash % 3 = 2</text>

  <!-- 标注: 有序 ③ -->
  <line x1="457" y1="42" x2="457" y2="28" stroke="#e65100" stroke-width="1" stroke-dasharray="3,2"/>
  <circle cx="457" cy="24" r="7" fill="#e65100"/>
  <text x="457" y="21" text-anchor="middle" font-size="9" font-weight="bold" fill="#fff">3</text>
  <text x="457" y="16" text-anchor="middle" font-size="7" fill="#e65100" font-weight="bold">有序</text>

  <line x1="555" y1="68" x2="590" y2="68" stroke="#1565c0" stroke-width="1.5" marker-end="url(#q100-arrow-blue)"/>

  <!-- 第5层: Consumer + 存储层 -->
  <rect x="592" y="38" width="120" height="60" rx="6" fill="#e8f5e9" stroke="#3c8772" stroke-width="1.5" filter="url(#q100-shadow)"/>
  <text x="652" y="53" text-anchor="middle" font-size="10" font-weight="bold" fill="#2e6b5a">存储层 Consumer</text>
  <text x="652" y="66" text-anchor="middle" font-size="8" fill="#546e7a">手动提交 + 幂等</text>
  <text x="652" y="79" text-anchor="middle" font-size="7" fill="#546e7a">Redis(Hot)+MySQL(Shard)</text>
  <text x="652" y="92" text-anchor="middle" font-size="7" fill="#546e7a">+HBase(Cold Archive)</text>

  <!-- 标注: 不重 ② -->
  <line x1="652" y1="38" x2="652" y2="28" stroke="#e53935" stroke-width="1" stroke-dasharray="3,2"/>
  <circle cx="652" cy="24" r="7" fill="#e53935"/>
  <text x="652" y="21" text-anchor="middle" font-size="9" font-weight="bold" fill="#fff">2</text>
  <text x="652" y="16" text-anchor="middle" font-size="7" fill="#e53935" font-weight="bold">不重</text>

  <line x1="720" y1="68" x2="720" y2="155" stroke="#1565c0" stroke-width="1.5"/>

  <!-- ===== 第6层: 推送层 (第二行，接上面) ===== -->
  <line x1="8" y1="110" x2="8" y2="155" stroke="#7b1fa2" stroke-width="1.5"/>

  <rect x="8" y="140" width="80" height="38" rx="6" fill="url(#q100-grad-purple)" filter="url(#q100-shadow)"/>
  <text x="48" y="157" text-anchor="middle" font-size="10" font-weight="bold" fill="#fff">Push Service</text>
  <text x="48" y="172" text-anchor="middle" font-size="8" fill="#e1bee7">分发 + WebSocket</text>

  <!-- 标注: 低延迟 ④ -->
  <circle cx="48" cy="135" r="7" fill="#7b1fa2"/>
  <text x="48" y="132" text-anchor="middle" font-size="9" font-weight="bold" fill="#fff">4</text>
  <text x="48" y="125" text-anchor="middle" font-size="7" fill="#7b1fa2" font-weight="bold">低延迟</text>

  <line x1="88" y1="159" x2="120" y2="159" stroke="#7b1fa2" stroke-width="1.5" marker-end="url(#q100-arrow-purple)"/>

  <!-- WebSocket 连接管理器 -->
  <rect x="122" y="140" width="95" height="38" rx="6" fill="#fff" stroke="#7b1fa2" stroke-width="1.5" filter="url(#q100-shadow)"/>
  <text x="169" y="155" text-anchor="middle" font-size="10" font-weight="bold" fill="#4a148c">WebSocket</text>
  <text x="169" y="172" text-anchor="middle" font-size="8" fill="#78909c">长连接池 10w+</text>

  <line x1="217" y1="159" x2="248" y2="159" stroke="#7b1fa2" stroke-width="1.5" marker-end="url(#q100-arrow-purple)"/>

  <!-- 用户B 接收 -->
  <rect x="250" y="140" width="62" height="38" rx="6" fill="url(#q100-grad-purple)" filter="url(#q100-shadow)"/>
  <text x="281" y="157" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">用户B</text>
  <text x="281" y="172" text-anchor="middle" font-size="8" fill="#e1bee7">接收方</text>

  <!-- 消息流向箭头 -->
  <line x1="281" y1="140" x2="281" y2="110" stroke="#90a4ae" stroke-width="0.8" stroke-dasharray="4,3"/>
  <text x="310" y="118" font-size="7" fill="#90a4ae">ACK 回执</text>

  <!-- ===== 第7层: Redis 缓存标注 ===== -->
  <rect x="340" y="135" width="200" height="48" rx="6" fill="#e8eaf6" stroke="#1565c0" stroke-width="1" filter="url(#q100-shadow)"/>
  <text x="440" y="151" text-anchor="middle" font-size="10" font-weight="bold" fill="#1565c0">Redis 热缓存层</text>
  <text x="440" y="166" text-anchor="middle" font-size="8" fill="#546e7a">SETNX messageId TTL 24h（去重）</text>
  <text x="440" y="179" text-anchor="middle" font-size="8" fill="#546e7a">ZSET 最近消息缓存（100条/会话）</text>

  <!-- ===== 群聊 Fanout 区域 ===== -->
  <rect x="555" y="100" width="158" height="120" rx="6" fill="#fff3e0" stroke="#e65100" stroke-width="1" filter="url(#q100-shadow)"/>
  <text x="634" y="116" text-anchor="middle" font-size="11" font-weight="bold" fill="#e65100">群聊 Fanout 扩展</text>

  <rect x="565" y="125" width="138" height="16" rx="3" fill="#e65100" opacity="0.15"/>
  <text x="634" y="137" text-anchor="middle" font-size="8" fill="#e65100">1条群消息 → N个用户队列</text>

  <rect x="565" y="146" width="42" height="14" rx="2" fill="#e65100" opacity="0.3"/>
  <text x="586" y="157" text-anchor="middle" font-size="7" fill="#e65100">U1</text>
  <rect x="612" y="146" width="42" height="14" rx="2" fill="#e65100" opacity="0.3"/>
  <text x="633" y="157" text-anchor="middle" font-size="7" fill="#e65100">U2</text>
  <rect x="659" y="146" width="44" height="14" rx="2" fill="#e65100" opacity="0.3"/>
  <text x="681" y="157" text-anchor="middle" font-size="7" fill="#e65100">U3...</text>

  <rect x="565" y="165" width="138" height="14" rx="3" fill="#e53935" opacity="0.15"/>
  <text x="634" y="175" text-anchor="middle" font-size="8" fill="#e53935">已读回执 → 单独 Topic</text>

  <text x="634" y="195" text-anchor="middle" font-size="7" fill="#78909c">群聊分发: 写入 N 个用户</text>
  <text x="634" y="208" text-anchor="middle" font-size="7" fill="#78909c">的 session queue 队列</text>

  <!-- ===== 四大需求详解区域 ===== -->
  <rect x="10" y="200" width="700" height="230" rx="6" fill="#fff" stroke="#cfd8dc" stroke-width="1" filter="url(#q100-shadow)"/>
  <text x="360" y="220" text-anchor="middle" font-size="13" font-weight="bold" fill="#37474f">四大可靠性需求 — 逐层覆盖</text>

  <!-- 不丢 -->
  <rect x="22" y="233" width="162" height="88" rx="5" fill="#e8f5e9" stroke="#3c8772" stroke-width="1.2"/>
  <circle cx="42" cy="248" r="9" fill="#3c8772"/>
  <text x="42" y="252" text-anchor="middle" font-size="10" font-weight="bold" fill="#fff">1</text>
  <text x="55" y="252" font-size="12" font-weight="bold" fill="#2e6b5a">不丢失 (At-Least-Once)</text>
  <text x="32" y="270" font-size="9" fill="#546e7a">- Producer: acks=all + retries=3</text>
  <text x="32" y="284" font-size="9" fill="#546e7a">- 本地消息表：DB 事务保证</text>
  <text x="32" y="298" font-size="9" fill="#546e7a">- Broker: replication.factor=3</text>
  <text x="32" y="312" font-size="9" fill="#546e7a">- min.insync.replicas=2</text>

  <!-- 不重 -->
  <rect x="193" y="233" width="162" height="88" rx="5" fill="#ffebee" stroke="#e53935" stroke-width="1.2"/>
  <circle cx="213" cy="248" r="9" fill="#e53935"/>
  <text x="213" y="252" text-anchor="middle" font-size="10" font-weight="bold" fill="#fff">2</text>
  <text x="226" y="252" font-size="12" font-weight="bold" fill="#c62828">不重复 (Exactly-Once)</text>
  <text x="203" y="270" font-size="9" fill="#546e7a">- 雪花算法 messageId 全局唯一</text>
  <text x="203" y="284" font-size="9" fill="#546e7a">- Redis SETNX 幂等去重 TTL 24h</text>
  <text x="203" y="298" font-size="9" fill="#546e7a">- Consumer 手动提交 Offset</text>
  <text x="203" y="312" font-size="9" fill="#546e7a">- DB 唯一索引兜底</text>

  <!-- 有序 -->
  <rect x="364" y="233" width="162" height="88" rx="5" fill="#fff3e0" stroke="#e65100" stroke-width="1.2"/>
  <circle cx="384" cy="248" r="9" fill="#e65100"/>
  <text x="384" y="252" text-anchor="middle" font-size="10" font-weight="bold" fill="#fff">3</text>
  <text x="397" y="252" font-size="12" font-weight="bold" fill="#bf360c">有序性 (Ordering)</text>
  <text x="374" y="270" font-size="9" fill="#546e7a">- sessionId hash → 同一 Partition</text>
  <text x="374" y="284" font-size="9" fill="#546e7a">- 单 Partition 内 offset 递增</text>
  <text x="374" y="298" font-size="9" fill="#546e7a">- 单线程消费保证顺序处理</text>
  <text x="374" y="312" font-size="9" fill="#546e7a">- 消息携带 seq 序号</text>

  <!-- 低延迟 -->
  <rect x="535" y="233" width="162" height="88" rx="5" fill="#f3e5f5" stroke="#7b1fa2" stroke-width="1.2"/>
  <circle cx="555" cy="248" r="9" fill="#7b1fa2"/>
  <text x="555" y="252" text-anchor="middle" font-size="10" font-weight="bold" fill="#fff">4</text>
  <text x="568" y="252" font-size="12" font-weight="bold" fill="#4a148c">低延迟 (Low Latency)</text>
  <text x="545" y="270" font-size="9" fill="#546e7a">- linger.ms=0 立即发送</text>
  <text x="545" y="284" font-size="9" fill="#546e7a">- acks=1 降低确认延迟</text>
  <text x="545" y="298" font-size="9" fill="#546e7a">- WebSocket 长连接 Push</text>
  <text x="545" y="312" font-size="9" fill="#546e7a">- Redis 热路径缓存</text>

  <!-- 存储三层架构 -->
  <rect x="22" y="332" width="332" height="88" rx="5" fill="#e3f2fd" stroke="#1565c0" stroke-width="1.2"/>
  <text x="188" y="348" text-anchor="middle" font-size="11" font-weight="bold" fill="#0d47a1">存储三层架构</text>

  <rect x="32" y="356" width="95" height="28" rx="3" fill="#e53935" opacity="0.85"/>
  <text x="79" y="370" text-anchor="middle" font-size="9" font-weight="bold" fill="#fff">Redis 热层</text>
  <text x="79" y="382" text-anchor="middle" font-size="7" fill="#ffcdd2">最近 100 条</text>

  <rect x="137" y="356" width="95" height="28" rx="3" fill="#1565c0" opacity="0.85"/>
  <text x="184" y="370" text-anchor="middle" font-size="9" font-weight="bold" fill="#fff">MySQL 温层</text>
  <text x="184" y="382" text-anchor="middle" font-size="7" fill="#bbdefb">分库分表 100库</text>

  <rect x="242" y="356" width="100" height="28" rx="3" fill="#7b1fa2" opacity="0.85"/>
  <text x="292" y="370" text-anchor="middle" font-size="9" font-weight="bold" fill="#fff">HBase 冷层</text>
  <text x="292" y="382" text-anchor="middle" font-size="7" fill="#e1bee7">归档 90 天</text>

  <text x="188" y="408" text-anchor="middle" font-size="8" fill="#78909c">user_id % 100 → DB 路由 | 近期 Redis 命中 → 快速拉取</text>

  <!-- 容量估算 -->
  <rect x="364" y="332" width="333" height="88" rx="5" fill="#f5f5f5" stroke="#90a4ae" stroke-width="1.2"/>
  <text x="530" y="348" text-anchor="middle" font-size="11" font-weight="bold" fill="#37474f">容量估算（日活 1000W）</text>
  <text x="530" y="366" text-anchor="middle" font-size="9" fill="#546e7a">单日消息量: 1000W × 50条 ≈ 5亿条/天</text>
  <text x="530" y="382" text-anchor="middle" font-size="9" fill="#546e7a">Kafka: 5亿 × 1KB ≈ 500GB/天 → 7天保留 ≈ 3.5TB</text>
  <text x="530" y="398" text-anchor="middle" font-size="9" fill="#546e7a">MySQL: 100库 × 100表 → 每表约 5W条/天 → 2年 ≈ 1.8亿条</text>
  <text x="530" y="414" text-anchor="middle" font-size="9" fill="#546e7a">WebSocket: 1000W × 10%在线 ≈ 100W 长连接</text>

  <!-- ===== 底部总结 ===== -->
  <rect x="10" y="445" width="700" height="52" rx="6" fill="url(#q100-grad-blue)" filter="url(#q100-shadow)"/>
  <text x="360" y="466" text-anchor="middle" font-size="12" font-weight="bold" fill="#fff">端到端消息流: 用户A → Gateway → MsgSvc → Kafka → Consumer → Store + Push → 用户B</text>
  <text x="360" y="486" text-anchor="middle" font-size="11" fill="#bbdefb">不丢(acks=all+本地消息表) · 不重(雪花ID+SETNX) · 有序(hash Partition) · 低延迟(WebSocket+Redis)</text>

  <!-- 下半区右侧: 已读回执 -->
  <rect x="10" y="508" width="340" height="52" rx="5" fill="#fce4ec" stroke="#e53935" stroke-width="1"/>
  <text x="180" y="526" text-anchor="middle" font-size="11" font-weight="bold" fill="#c62828">已读回执流程</text>
  <text x="180" y="544" text-anchor="middle" font-size="9" fill="#546e7a">用户B 收到消息 → ACK 回执 → 写入 read-receipt Topic → 用户A 收到"已读"状态</text>
  <text x="180" y="556" text-anchor="middle" font-size="8" fill="#90a4ae">独立 Topic 避免影响主消息通道 ｜ 异步处理</text>

  <!-- 下半区右侧: 扩展性 -->
  <rect x="360" y="508" width="350" height="52" rx="5" fill="#e8f5e9" stroke="#3c8772" stroke-width="1"/>
  <text x="535" y="526" text-anchor="middle" font-size="11" font-weight="bold" fill="#2e6b5a">扩展性要点</text>
  <text x="535" y="544" text-anchor="middle" font-size="9" fill="#546e7a">Kafka Partition 水平扩展 | 消费者组动态 Rebalance | MySQL 分库数按 userId 取模</text>
  <text x="535" y="556" text-anchor="middle" font-size="8" fill="#90a4ae">新增分库 → 数据迁移 → 灰度切换 ｜ 提前规划分库数</text>

  <!-- 箭头连接 -->
  <line x1="720" y1="432" x2="720" y2="720" stroke="#cfd8dc" stroke-width="0.5"/>
  <line x1="0" y1="432" x2="0" y2="720" stroke="#cfd8dc" stroke-width="0.5"/>
</svg>

从用户 A 发送消息到用户 B 接收，整个链路经过 7 层处理：Gateway 鉴权限流 → Message Service 生成雪花 ID 并落地本地消息表 → Kafka 按 sessionId hash 分区 → Consumer 手动提交 + 幂等去重 → 三层存储（Redis 热 / MySQL 温 / HBase 冷）→ Push Service 通过 WebSocket 长连接推送到用户 B。四大需求每层都有对应保障。

## IM 系统核心代码实现

### Layer 1 — 消息服务发消息（雪花 ID + 本地消息表 + KafkaTemplate）

```java
// MessageService.java — 消息发送核心服务
@Service
public class MessageService {

    @Autowired private SnowflakeIdGenerator idGenerator; // 雪花算法
    @Autowired private LocalMessageTableMapper msgMapper; // 本地消息表
    @Autowired private KafkaTemplate<String, Message> kafkaTemplate;
    @Autowired private RedisTemplate<String, String> redisTemplate;

    /**
     * 发送消息 — 保证不丢的核心流程
     */
    @Transactional(rollbackFor = Exception.class)
    public SendResult sendMessage(SendRequest req) {
        // Step 1: 雪花算法生成全局唯一 messageId
        long messageId = idGenerator.nextId();

        // Step 2: 写入本地消息表（与业务在同一事务）
        MessageEntity entity = new MessageEntity();
        entity.setMessageId(messageId);
        entity.setSessionId(req.getSessionId());
        entity.setSenderId(req.getSenderId());
        entity.setReceiverId(req.getReceiverId());
        entity.setContent(req.getContent());
        entity.setStatus(MessageStatus.PENDING);  // 待发送
        entity.setCreateTime(System.currentTimeMillis());
        msgMapper.insert(entity);  // INSERT INTO message_local(...)

        // Step 3: 投递到 Kafka（带回调处理失败）
        // 按 sessionId hash 到固定 Partition — 保证有序
        ProducerRecord<String, Message> record = new ProducerRecord<>(
            "im-message-topic",                          // Topic
            req.getSessionId(),                          // Key = sessionId
            buildMessageDTO(entity)                       // Value
        );

        kafkaTemplate.send(record).addCallback(
            success -> {
                // 发送成功 → 更新本地消息表状态
                msgMapper.updateStatus(messageId, MessageStatus.SENT);
                log.info("Message sent to Kafka: messageId={}, offset={}",
                    messageId, success.getRecordMetadata().offset());
            },
            failure -> {
                // 发送失败 → 标记失败，定时任务重试
                msgMapper.updateStatus(messageId, MessageStatus.SEND_FAILED);
                log.error("Failed to send message: messageId={}", messageId, failure);
            }
        );

        return SendResult.builder()
            .messageId(messageId)
            .status(SendStatus.ACCEPTED)
            .build();
    }
}
```

### Layer 2 — 消费者保证不丢不重（手动提交 + Redis SETNX 去重 + 幂等）

```java
// MessageConsumer.java — 消费端可靠性保障
@Component
public class MessageConsumer {

    @Autowired private RedisTemplate<String, String> redisTemplate;
    @Autowired private MessageStoreService storeService;
    @Autowired private PushService pushService;

    /**
     * 消费消息 — 手动提交 Offset + 幂等去重
     * 不丢: 手动提交，只有处理成功才 commit
     * 不重: Redis SETNX 幂等校验，重复消息直接跳过
     */
    @KafkaListener(topics = "im-message-topic", concurrency = "6")
    public void consume(ConsumerRecord<String, Message> record,
                         Acknowledgment ack) {
        Message msg = record.value();
        String dedupKey = "msg:dedup:" + msg.getMessageId();

        try {
            // Step 1: Redis SETNX 幂等去重 — TTL 24小时
            // SETNX 返回 true 表示 key 不存在，首次处理
            // SETNX 返回 false 表示已处理过，跳过（不重）
            Boolean firstTime = redisTemplate.opsForValue()
                .setIfAbsent(dedupKey, "1", Duration.ofHours(24));

            if (Boolean.FALSE.equals(firstTime)) {
                // 重复消息 — 直接确认，不重复处理
                log.warn("Duplicate message ignored: messageId={}", msg.getMessageId());
                ack.acknowledge();  // 仍然 ack，避免重试
                return;
            }

            // Step 2: 幂等写 DB — messageId 作为唯一索引兜底
            boolean stored = storeService.tryInsert(msg);
            if (!stored) {
                // DB 唯一索引冲突，重复消息
                log.warn("DB unique index conflict: messageId={}", msg.getMessageId());
                ack.acknowledge();
                return;
            }

            // Step 3: Push 给接收方
            pushService.pushToUser(msg.getReceiverId(), msg);

            // Step 4: 处理成功后手动提交 Offset
            ack.acknowledge();

        } catch (Exception e) {
            // 异常不 ack — Kafka 会重新投递（At-Least-Once）
            // 下次消费时 SETNX 和 DB 唯一索引保证幂等
            log.error("Consumer error, will retry: messageId={}",
                msg.getMessageId(), e);
            throw e;  // 抛异常，Kafka 重试
        }
    }
}
```

### Layer 3 — 有序性保证（sessionId hash 到 Partition + 单线程消费）

```java
// 有序性设计 — 三层保障

// ===== 第1层: Producer 按 sessionId 路由到固定 Partition =====
// 已在 Layer 1 的 sendMessage 中实现:
// new ProducerRecord<>("im-message-topic", sessionId, message)
// Kafka 默认按 key hash: Math.abs(sessionId.hashCode()) % partitionCount
// 同一 session 的所有消息必然进入同一个 Partition

// ===== 第2层: 消费者单线程处理每个 Partition =====
// Kafka 保证同一 Partition 的消息由 Consumer Group 中同一 Consumer 处理
// concurrency=6 启动 6 个线程，每个线程独立处理分配的 Partition
// 单 Partition 内消息按 offset 顺序消费，天然有序

// ===== 第3层: 消息序列号辅助排序 =====
@Data
public class Message {
    private Long messageId;       // 雪花 ID（全局递增）
    private String sessionId;     // 会话 ID → hash key
    private Long seq;             // 会话内序列号，用于客户端排序
    private String senderId;
    private String receiverId;
    private String content;
    private Long timestamp;

    /**
     * Producer 侧生成递增序列号
     * 使用 Redis INCR 每次发送前递增
     */
    public static Message create(String sessionId, String senderId,
                                  String content, StringRedisTemplate redis) {
        Long seq = redis.opsForValue()
            .increment("seq:" + sessionId);  // 会话内自增序号
        Message msg = new Message();
        msg.setMessageId(new SnowflakeIdGenerator().nextId());
        msg.setSessionId(sessionId);
        msg.setSeq(seq);           // 接收方按 seq 排序展示
        msg.setSenderId(senderId);
        msg.setContent(content);
        msg.setTimestamp(System.currentTimeMillis());
        return msg;
    }
}
```

### Layer 4 — 低延迟推送（WebSocket 长连接 + Redis 缓存）

```java
// PushService.java — 低延迟消息推送
@Service
public class PushService {

    // userId → WebSocketSession 映射
    private final ConcurrentHashMap<String, WebSocketSession> sessionMap
        = new ConcurrentHashMap<>();

    @Autowired private StringRedisTemplate redisTemplate;

    /**
     * 推送消息给接收方 — 低延迟核心
     */
    public void pushToUser(String userId, Message msg) {
        // Step 1: 写入 Redis 热缓存（ZSET — 最近 100 条消息）
        String cacheKey = "msg:recent:" + msg.getSessionId();
        redisTemplate.opsForZSet().add(cacheKey,
            JSON.toJSONString(msg), msg.getTimestamp());
        // 只保留最近 100 条
        redisTemplate.opsForZSet()
            .removeRange(cacheKey, 0, -101);

        // Step 2: WebSocket 实时推送
        WebSocketSession session = sessionMap.get(userId);
        if (session != null && session.isOpen()) {
            try {
                session.sendMessage(new TextMessage(
                    JSON.toJSONString(msg)));
            } catch (IOException e) {
                log.error("WebSocket push failed: userId={}", userId, e);
                sessionMap.remove(userId);  // 移除失效连接
            }
        }
        // 如果用户不在线: 消息已持久化，上线后从 Redis/DB 拉取
    }

    // WebSocket 连接管理
    @OnOpen
    public void onOpen(Session session) {
        String userId = getUserIdFromSession(session);
        sessionMap.put(userId, session);
        log.info("WS connected: userId={}, total={}", userId, sessionMap.size());
    }

    @OnClose
    public void onClose(Session session) {
        String userId = getUserIdFromSession(session);
        sessionMap.remove(userId);
    }
}

// === Kafka Producer 低延迟配置 ===
@Configuration
public class KafkaProducerConfig {

    @Bean
    public ProducerFactory<String, Message> producerFactory() {
        Map<String, Object> props = new HashMap<>();
        props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, "kafka1:9092,kafka2:9092");
        props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG,
            StringSerializer.class);
        props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG,
            JsonSerializer.class);

        // === 低延迟关键配置 ===
        props.put(ProducerConfig.LINGER_MS_CONFIG, 0);       // 不等待，立即发送
        props.put(ProducerConfig.ACKS_CONFIG, "1");          // Leader 确认即可
        props.put(ProducerConfig.COMPRESSION_TYPE_CONFIG, "lz4"); // 快速压缩
        props.put(ProducerConfig.BATCH_SIZE_CONFIG, 16384);  // 16KB 小批次
        props.put(ProducerConfig.MAX_IN_FLIGHT_REQUESTS_PER_CONNECTION, 1); // 有序

        return new DefaultKafkaProducerFactory<>(props);
    }
}
```

### Layer 5 — 群聊 Fanout（一条消息写入 N 个用户队列 + 已读回执）

```java
// GroupChatService.java — 群聊消息分发
@Service
public class GroupChatService {

    @Autowired private KafkaTemplate<String, Message> kafkaTemplate;
    @Autowired private GroupMemberService groupMemberService;
    @Autowired private RedisTemplate<String, String> redisTemplate;

    /**
     * 群聊发送 — 一条消息 Fanout 到 N 个用户队列
     */
    @Transactional
    public void sendGroupMessage(GroupMessageRequest req) {
        // Step 1: 获取群成员列表（从 Redis 缓存）
        List<String> memberIds = groupMemberService
            .getGroupMembers(req.getGroupId());

        // Step 2: 雪花 ID
        SnowflakeIdGenerator idGen = new SnowflakeIdGenerator();

        // Step 3: 为每个群成员写入消息到对应的用户队列 Topic
        for (String memberId : memberIds) {
            // 跳过发送者自己
            if (memberId.equals(req.getSenderId())) continue;

            Message msg = Message.builder()
                .messageId(idGen.nextId())
                .groupId(req.getGroupId())
                .senderId(req.getSenderId())
                .receiverId(memberId)           // 每个成员作为接收方
                .sessionId("group:" + req.getGroupId())
                .content(req.getContent())
                .messageType(MessageType.GROUP_CHAT)
                .timestamp(System.currentTimeMillis())
                .build();

            // 每个用户的消息写入各自 session Queue 的 Kafka Topic
            // 有序性: 按 (groupId + memberId) hash → 同一 Partition
            String routingKey = req.getGroupId() + ":" + memberId;
            kafkaTemplate.send(
                "im-message-topic",             // 主消息 Topic
                routingKey,                     // 路由 Key
                msg
            );
        }

        log.info("Group message fanout: groupId={}, members={}",
            req.getGroupId(), memberIds.size() - 1);
    }
}

// ReadReceiptService.java — 已读回执独立处理
@Service
public class ReadReceiptService {

    @Autowired private KafkaTemplate<String, ReadReceipt> kafkaTemplate;

    /**
     * 已读回执 — 独立 Topic，不影响主消息通道
     */
    public void sendReadReceipt(String userId, String sessionId, Long lastReadMsgId) {
        ReadReceipt receipt = ReadReceipt.builder()
            .userId(userId)
            .sessionId(sessionId)
            .lastReadMessageId(lastReadMsgId)
            .readTime(System.currentTimeMillis())
            .build();

        // 投递到独立的 read-receipt Topic
        kafkaTemplate.send("read-receipt-topic", sessionId, receipt);

        // 消费者侧: 更新 Redis ZSET 中消息的 read 状态
        // 发送方查询时: 从 Redis 获取最新已读位置
    }
}
```

| 需求维度 | 实现方案 | 关键配置 |
|---|---|---|
| **不丢失** | acks=all + 本地消息表 + Consumer 手动提交 + retries=3 | `acks=all`, `min.insync.replicas=2`, `enable.auto.commit=false`, `replication.factor=3` |
| **不重复** | 雪花算法 messageId + Redis SETNX 去重 TTL 24h + DB 唯一索引兜底 | `SETNX dedup_key TTL 86400`, `UNIQUE KEY uk_message_id` |
| **有序性** | Producer 按 sessionId hash → 同一 Partition + 单线程消费 + seq 序号 | `ProducerRecord(key=sessionId)`, `concurrency=1 per partition`, `max.in.flight.requests=1` |
| **低延迟** | Kafka linger.ms=0 + acks=1 + WebSocket 长连接 Push + Redis 热缓存 | `linger.ms=0`, `acks=1`, `WebSocket session pool`, `ZSET recent 100` |
| **高可用** | 多 Broker + 3 副本 + Consumer Group Rebalance + 异地多活 | `replication.factor=3`, `group.id`, `session.timeout.ms=30000` |
| **存储分层** | Redis 热层(100条) → MySQL 分表(100库×100表) → HBase 冷归档(90天) | `user_id % 100 → DB`, `TTL 冷数据迁移`, `ZSET 热缓存` |
| **群聊 Fanout** | 1 条群消息 → N 条独立消息写入用户队列 | 异步批量写，防止群聊放大效应 |
| **已读回执** | 独立 Topic + 异步处理 + Redis 状态缓存 | `read-receipt-topic` 独立，不阻塞主通道 |

## 一句话总结

**日活千万 IM 系统的可靠性设计围绕"不丢(acks=all+本地消息表)、不重(雪花ID+SETNX去重)、有序(sessionId hash到同一Partition)、低延迟(linger.ms=0+WebSocket)"四大核心展开，存储采用 Redis 热缓存 + MySQL 分库分表 + HBase 冷归档三层架构，群聊通过 Fanout 模式将一条消息分发到 N 个用户队列，已读回执使用独立 Topic 异步处理，从而在保证消息可靠性的同时做到端到端延迟低于 100ms。**

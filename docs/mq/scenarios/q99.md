# Q99 跨机房同步

跨机房 Kafka 同步的核心方案有四种：MirrorMaker 2（开源推荐，Kafka 2.4+ 内置）、Confluent Replicator（商业版 Exactly-Once）、Cluster Linking（Kafka 3.6+ 原生轻量）、自研 Connect Source/Sink Connector，其中 MirrorMaker 2 是企业最常用的方案，配合单写多读架构可以大幅降低复杂度。

## 跨机房同步架构

<svg viewBox="0 0 720 640" xmlns="http://www.w3.org/2000/svg" font-family="PingFang SC, Microsoft YaHei, sans-serif">
  <defs>
    <marker id="q99-arrow-blue" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#1565c0"/></marker>
    <marker id="q99-arrow-green" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#3c8772"/></marker>
    <marker id="q99-arrow-red" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#e53935"/></marker>
    <marker id="q99-arrow-orange" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#e65100"/></marker>
    <marker id="q99-arrow-purple" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#7b1fa2"/></marker>
    <filter id="q99-shadow"><feDropShadow dx="1" dy="2" stdDeviation="2" flood-opacity="0.15"/></filter>
    <linearGradient id="q99-grad-blue" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#1565c0"/><stop offset="100%" stop-color="#0d47a1"/></linearGradient>
    <linearGradient id="q99-grad-green" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#3c8772"/><stop offset="100%" stop-color="#2e6b5a"/></linearGradient>
  </defs>

  <!-- 背景 -->
  <rect width="720" height="640" fill="#fafbfc" rx="8"/>

  <!-- 标题 -->
  <text x="360" y="26" text-anchor="middle" font-size="16" font-weight="bold" fill="#1565c0">跨机房 Kafka 同步架构</text>

  <!-- ===== 北京机房 (x=15 y=50-300 w=310) ===== -->
  <rect x="15" y="50" width="310" height="250" rx="8" fill="#e3f2fd" stroke="#1565c0" stroke-width="1.5" filter="url(#q99-shadow)"/>
  <text x="170" y="72" text-anchor="middle" font-size="12" font-weight="bold" fill="#0d47a1">北京机房（Primary — 主写）</text>

  <rect x="28" y="82" width="284" height="40" rx="6" fill="url(#q99-grad-blue)" filter="url(#q99-shadow)"/>
  <text x="170" y="100" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Kafka Cluster A（主集群）</text>
  <text x="170" y="115" text-anchor="middle" font-size="9" fill="#bbdefb">broker-1, broker-2, broker-3 | 3 Replicas</text>

  <rect x="28" y="130" width="284" height="20" rx="4" fill="#fff" stroke="#90a4ae" stroke-width="0.8"/>
  <text x="170" y="144" text-anchor="middle" font-size="10" fill="#546e7a">Topic: orders (Partition 0..2)</text>

  <rect x="28" y="155" width="284" height="20" rx="4" fill="#fff" stroke="#90a4ae" stroke-width="0.8"/>
  <text x="170" y="169" text-anchor="middle" font-size="10" fill="#546e7a">Topic: payments (Partition 0..2)</text>

  <rect x="28" y="180" width="284" height="20" rx="4" fill="#fff" stroke="#90a4ae" stroke-width="0.8"/>
  <text x="170" y="194" text-anchor="middle" font-size="10" fill="#546e7a">Topic: inventory (Partition 0..2)</text>

  <rect x="48" y="212" width="244" height="24" rx="4" fill="#1565c0" opacity="0.9"/>
  <text x="170" y="228" text-anchor="middle" font-size="10" fill="#fff">Producers → 写入北京集群</text>

  <text x="170" y="254" text-anchor="middle" font-size="10" fill="#1565c0">地域内延迟: 1-5ms | acks=all</text>
  <text x="170" y="270" text-anchor="middle" font-size="9" fill="#78909c">生产流量仅进 A 集群，避免多主冲突</text>

  <!-- ===== 同步引擎中心列 (x=340 y=50-300 w=85) ===== -->
  <rect x="340" y="50" width="85" height="250" rx="8" fill="#e8f5e9" stroke="#3c8772" stroke-width="1.5" filter="url(#q99-shadow)"/>
  <text x="382" y="72" text-anchor="middle" font-size="10" font-weight="bold" fill="#2e6b5a">同步引擎</text>

  <!-- MM2 box 80x40 -->
  <rect x="343" y="82" width="80" height="40" rx="6" fill="url(#q99-grad-green)" filter="url(#q99-shadow)"/>
  <text x="383" y="100" text-anchor="middle" font-size="10" font-weight="bold" fill="#fff">MM2</text>
  <text x="383" y="115" text-anchor="middle" font-size="8" fill="#c8e6c9">Kafka 2.4+</text>

  <text x="382" y="134" text-anchor="middle" font-size="8" font-weight="bold" fill="#3c8772">方案一: MM2 ✓</text>
  <text x="382" y="146" text-anchor="middle" font-size="8" fill="#546e7a">offset 转换</text>
  <text x="382" y="157" text-anchor="middle" font-size="8" fill="#546e7a">ACL 同步</text>
  <text x="382" y="168" text-anchor="middle" font-size="8" fill="#546e7a">topic 映射</text>

  <rect x="343" y="180" width="80" height="20" rx="3" fill="#e65100" opacity="0.85"/>
  <text x="383" y="194" text-anchor="middle" font-size="8" fill="#fff">方案二: CLink</text>

  <text x="383" y="210" text-anchor="middle" font-size="7" fill="#e65100">Kafka 3.6+</text>

  <rect x="343" y="224" width="80" height="20" rx="3" fill="#7b1fa2" opacity="0.85"/>
  <text x="383" y="238" text-anchor="middle" font-size="8" fill="#fff">方案三: Repl</text>

  <text x="383" y="254" text-anchor="middle" font-size="7" fill="#7b1fa2">商业版</text>

  <text x="383" y="275" text-anchor="middle" font-size="8" fill="#78909c">异步同步</text>
  <text x="383" y="288" text-anchor="middle" font-size="8" fill="#e53935" font-weight="bold">注意回环!</text>

  <!-- ===== 上海机房 (x=440 y=50-300 w=270) ===== -->
  <rect x="440" y="50" width="270" height="250" rx="8" fill="#fce4ec" stroke="#e53935" stroke-width="1.5" filter="url(#q99-shadow)"/>
  <text x="575" y="72" text-anchor="middle" font-size="12" font-weight="bold" fill="#c62828">上海机房（Replica — 读）</text>

  <rect x="453" y="82" width="244" height="40" rx="6" fill="#e53935" filter="url(#q99-shadow)"/>
  <text x="575" y="100" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Kafka Cluster B（副本集群）</text>
  <text x="575" y="115" text-anchor="middle" font-size="9" fill="#ffcdd2">broker-1, broker-2, broker-3</text>

  <rect x="453" y="130" width="244" height="20" rx="4" fill="#fff" stroke="#90a4ae" stroke-width="0.8"/>
  <text x="575" y="144" text-anchor="middle" font-size="10" fill="#546e7a">A.orders (mirror from A)</text>

  <rect x="453" y="155" width="244" height="20" rx="4" fill="#fff" stroke="#90a4ae" stroke-width="0.8"/>
  <text x="575" y="169" text-anchor="middle" font-size="10" fill="#546e7a">A.payments (mirror from A)</text>

  <rect x="453" y="180" width="244" height="20" rx="4" fill="#fff" stroke="#90a4ae" stroke-width="0.8"/>
  <text x="575" y="194" text-anchor="middle" font-size="10" fill="#546e7a">A.inventory (mirror from A)</text>

  <rect x="473" y="212" width="204" height="24" rx="4" fill="#e53935" opacity="0.85"/>
  <text x="575" y="228" text-anchor="middle" font-size="10" fill="#fff">Consumers → 就近消费</text>

  <text x="575" y="254" text-anchor="middle" font-size="10" fill="#c62828">上海服务读取本地 B 集群</text>
  <text x="575" y="270" text-anchor="middle" font-size="9" fill="#78909c">不跨机房读，延迟低</text>

  <!-- 同步箭头 A→B (从北京右边穿到上海左边) -->
  <line x1="325" y1="145" x2="440" y2="145" stroke="#3c8772" stroke-width="2" marker-end="url(#q99-arrow-green)"/>
  <text x="383" y="138" text-anchor="middle" font-size="9" fill="#3c8772">异步同步 50-200ms</text>

  <!-- 回环风险箭头 B→A -->
  <line x1="440" y1="236" x2="325" y2="236" stroke="#e53935" stroke-width="2" stroke-dasharray="6,3" marker-end="url(#q99-arrow-red)"/>
  <text x="383" y="248" text-anchor="middle" font-size="8" fill="#e53935">双向同步有回环风险</text>

  <!-- 箭头 A→同步引擎 -->
  <line x1="325" y1="195" x2="340" y2="195" stroke="#3c8772" stroke-width="1.5" marker-end="url(#q99-arrow-green)"/>
  <!-- 箭头 同步引擎→B -->
  <line x1="425" y1="195" x2="440" y2="195" stroke="#3c8772" stroke-width="1.5" marker-end="url(#q99-arrow-green)"/>

  <!-- ===== 四项注意事项卡片 (y=315-430, 4x155x80) ===== -->

  <!-- 卡片 1: 延迟影响 -->
  <rect x="32" y="315" width="155" height="80" rx="6" fill="#fff3e0" stroke="#e65100" stroke-width="1"/>
  <text x="109" y="335" text-anchor="middle" font-size="11" font-weight="bold" fill="#e65100">延迟影响</text>
  <text x="109" y="351" text-anchor="middle" font-size="10" fill="#546e7a">跨机房 50-200ms</text>
  <text x="109" y="365" text-anchor="middle" font-size="10" fill="#546e7a">异步复制=最终一致</text>
  <text x="109" y="379" text-anchor="middle" font-size="10" fill="#546e7a">强一致场景不适合</text>

  <!-- 卡片 2: 回环风险 -->
  <rect x="199" y="315" width="155" height="80" rx="6" fill="#ffebee" stroke="#e53935" stroke-width="1"/>
  <text x="276" y="335" text-anchor="middle" font-size="11" font-weight="bold" fill="#e53935">回环风险</text>
  <text x="276" y="351" text-anchor="middle" font-size="10" fill="#546e7a">A→B→A 无限循环</text>
  <text x="276" y="365" text-anchor="middle" font-size="10" fill="#546e7a">解决方案: Topic 前缀</text>
  <text x="276" y="379" text-anchor="middle" font-size="10" fill="#1565c0">A.topic → B.A.topic ✓</text>

  <!-- 卡片 3: 磁盘容量 -->
  <rect x="366" y="315" width="155" height="80" rx="6" fill="#e8eaf6" stroke="#1565c0" stroke-width="1"/>
  <text x="443" y="335" text-anchor="middle" font-size="11" font-weight="bold" fill="#1565c0">磁盘容量</text>
  <text x="443" y="351" text-anchor="middle" font-size="10" fill="#546e7a">两端各存完整数据</text>
  <text x="443" y="365" text-anchor="middle" font-size="10" fill="#546e7a">存储翻倍</text>
  <text x="443" y="379" text-anchor="middle" font-size="10" fill="#546e7a">需预留 2x 磁盘空间</text>

  <!-- 卡片 4: 断网恢复 -->
  <rect x="533" y="315" width="155" height="80" rx="6" fill="#e8f5e9" stroke="#3c8772" stroke-width="1"/>
  <text x="610" y="335" text-anchor="middle" font-size="11" font-weight="bold" fill="#3c8772">断网恢复</text>
  <text x="610" y="351" text-anchor="middle" font-size="10" fill="#546e7a">网络中断时本地缓存</text>
  <text x="610" y="365" text-anchor="middle" font-size="10" fill="#546e7a">恢复后自动追赶 Lag</text>
  <text x="610" y="379" text-anchor="middle" font-size="10" fill="#546e7a">关注堆积量及恢复时间</text>

  <!-- ===== 建议栏 (y=445-485) ===== -->
  <rect x="15" y="445" width="690" height="40" rx="6" fill="#e3f2fd" stroke="#1565c0" stroke-width="1" filter="url(#q99-shadow)"/>
  <text x="360" y="462" text-anchor="middle" font-size="11" font-weight="bold" fill="#0d47a1">建议: 单写多读架构优于双向同步，北京集群写入，上海集群读取副本，避免回环和无序问题</text>
  <text x="360" y="478" text-anchor="middle" font-size="10" fill="#546e7a">MM2 + replication.policy.separator + source.cluster.alias 即可实现安全隔离</text>

  <!-- ===== 底部箭头 (y=510-550) ===== -->
  <line x1="170" y1="535" x2="382" y2="535" stroke="#1565c0" stroke-width="1.5" marker-end="url(#q99-arrow-blue)"/>
  <text x="276" y="528" text-anchor="middle" font-size="10" fill="#1565c0">生产端就近写入</text>

  <line x1="420" y1="535" x2="575" y2="535" stroke="#e53935" stroke-width="1.5" marker-end="url(#q99-arrow-red)"/>
  <text x="497" y="528" text-anchor="middle" font-size="10" fill="#e53935">消费端就近消费</text>
</svg>

MM2 作为 Kafka 原生的跨集群同步方案，通过 Connect 框架运行，可以从源集群消费消息写入目标集群，自动进行 Topic 重命名（按 `source.cluster.alias` 前缀）和 Offset 转换，有效防止双向同步时的无限回环问题。Cluster Linking 则是 Kafka 3.6+ 推出的更轻量方案，无需额外进程直接在 Broker 间建立 Link。

## 跨机房同步代码配置

### Layer 1 — MirrorMaker 2 配置（双向同步 + 防回环）

```properties
# connect-mirror-maker.properties — MirrorMaker 2 核心配置文件
# 集群别名定义
clusters = beijing, shanghai
beijing.bootstrap.servers = beijing-kafka-broker1:9092,beijing-kafka-broker2:9092,beijing-kafka-broker3:9092
shanghai.bootstrap.servers = shanghai-kafka-broker1:9092,shanghai-kafka-broker2:9092,shanghai-kafka-broker3:9092

# 双向同步配置（beijing ↔ shanghai）
beijing->shanghai.enabled = true
shanghai->beijing.enabled = true

# 防止回环 — 关键配置
# replication.policy.class 使用 DefaultReplicationPolicy
# 源集群别名作为前缀：beijing.topic.name → shanghai.beijing.topic.name
# ShangHai 的消费者只消费 shanghai.* 前缀 Topic，不会回到 Beijing
replication.policy.separator = "."

# 同步策略
sync.topic.acls.enabled = true           # 同步 ACL 权限
sync.topic.configs.enabled = true        # 同步 Topic 配置（分区数、副本数）
emit.heartbeats.enabled = true           # 心跳检测同步状态
emit.checkpoints.enabled = true          # Offset 检查点，用于故障恢复

# Offset 转换 — Consumer 可以在 B 集群找到对应 Offset
# Beijing 集群 offset=100 → ShangHai 集群对应 offset 通过 RemoteClusterUtils 转换
offset-syncs.topic.replication.factor = 3
checkpoints.topic.replication.factor = 3
heartbeats.topic.replication.factor = 3

# 性能调优
tasks.max = 16                           # 并行同步 Task 数
offset.flush.interval.ms = 10000         # Offset 刷新间隔
offset.flush.timeout.ms = 5000           # Offset 刷新超时
```

### Layer 2 — Cluster Linking 配置（Kafka 3.6+ 原生方案）

```shell
#!/bin/bash
# kafka-cluster-links.sh — 建立跨集群 Link（Kafka 3.6+）

# Step 1: 在 Shanghai 集群上创建指向 Beijing 的 Link
# 注意: Link 是单向的，从 Beijing（源）→ Shanghai（目标）
kafka-cluster-links.sh \
  --bootstrap-server shanghai-kafka-broker1:9092 \
  --create \
  --link-id beijing-to-shanghai-link \
  --config bootstrap.servers=beijing-kafka-broker1:9092,beijing-kafka-broker2:9092 \
  --config consumer.offset.sync.enable=true \
  --config consumer.offset.sync.ms=30000 \
  --config auto.create.mirror.topics.enable=true \
  --config auto.create.mirror.topics.filters='{"topicFilters": [\
    {"name": "orders", "patternType": "MATCH", "filterType": "INCLUDE"},\
    {"name": "payments", "patternType": "MATCH", "filterType": "INCLUDE"},\
    {"name": "inventory", "patternType": "MATCH", "filterType": "INCLUDE"}\
  ]}'

# Step 2: 查看 Link 状态
kafka-cluster-links.sh \
  --bootstrap-server shanghai-kafka-broker1:9092 \
  --list

# Step 3: 查看 Mirror Topic 同步延迟
kafka-cluster-links.sh \
  --bootstrap-server shanghai-kafka-broker1:9092 \
  --describe --link-id beijing-to-shanghai-link

# Step 4: 暂停 / 恢复 Link（用于维护）
kafka-cluster-links.sh --bootstrap-server shanghai-kafka-broker1:9092 \
  --pause --link-id beijing-to-shanghai-link

kafka-cluster-links.sh --bootstrap-server shanghai-kafka-broker1:9092 \
  --resume --link-id beijing-to-shanghai-link
```

### Layer 3 — 单写多读架构配置（推荐生产实践）

```java
// 单写多读架构 — 生产端只写北京，消费端就近消费
// 生产端配置（所有机房的服务统一写北京集群）
@Configuration
public class ProducerConfig {

    @Bean
    public KafkaTemplate<String, String> kafkaTemplate() {
        Map<String, Object> props = new HashMap<>();
        // 只有北京集群地址 — 单写
        props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG,
            "beijing-kafka-broker1:9092,beijing-kafka-broker2:9092");
        props.put(ProducerConfig.ACKS_CONFIG, "all");
        props.put(ProducerConfig.RETRIES_CONFIG, 3);
        props.put(ProducerConfig.COMPRESSION_TYPE_CONFIG, "lz4");
        return new KafkaTemplate<>(new DefaultKafkaProducerFactory<>(props));
    }
}

// 消费端配置（每个机房消费本地集群 — 就近消费）
@Configuration
public class ShanghaiConsumerConfig {

    @Bean
    public KafkaListenerContainerFactory<?> shanghaiFactory() {
        Map<String, Object> props = new HashMap<>();
        // Shanghai 消费者只连 Shanghai 本地集群
        props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG,
            "shanghai-kafka-broker1:9092,shanghai-kafka-broker2:9092");
        props.put(ConsumerConfig.GROUP_ID_CONFIG, "shanghai-order-service");
        props.put(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG, "earliest");

        ConcurrentKafkaListenerContainerFactory<String, String> factory =
            new ConcurrentKafkaListenerContainerFactory<>();
        factory.setConsumerFactory(new DefaultKafkaConsumerFactory<>(props));
        return factory;
    }

    // 消费 Mirror 过来的 Topic — 前缀为 beijing.
    @KafkaListener(topics = "beijing.orders", containerFactory = "shanghaiFactory")
    public void consumeFromMirror(String message) {
        // 上海服务就近消费本地 Mirror 数据，延迟低
        processOrder(message);
    }
}
```

### Layer 4 — 同步延迟监控与告警

```shell
#!/bin/bash
# mirror-lag-monitor.sh — 跨机房同步延迟监控脚本

# 获取 MirrorMaker 2 所有 Mirror Topic 的 Lag
# 通过 Kafka Consumer Group 命令查看 MM2 内部 Consumer Group Lag
MM2_GROUP="mirrormaker2-beijing->shanghai"
BOOTSTRAP="shanghai-kafka-broker1:9092"
ALERT_THRESHOLD=50000  # Lag 超过 5 万条触发告警

# 查询 MirrorMaker 2 消费者组的 Lag
kafka-consumer-groups.sh \
  --bootstrap-server $BOOTSTRAP \
  --group $MM2_GROUP \
  --describe 2>/dev/null | while read line; do
    # 解析 Lag 值
    LAG=$(echo "$line" | awk '{print $6}')
    TOPIC=$(echo "$line" | awk '{print $1}')

    if [[ "$LAG" =~ ^[0-9]+$ ]] && [ "$LAG" -gt $ALERT_THRESHOLD ]; then
      echo "[ALERT] Topic=$TOPIC Lag=$LAG exceeds threshold=$ALERT_THRESHOLD"
      # 触发告警 — 飞书 / PagerDuty / Prometheus
      curl -X POST "https://open.feishu.cn/open-apis/bot/v2/hook/xxx" \
        -H "Content-Type: application/json" \
        -d "{\"msg_type\":\"text\",\"content\":{\"text\":\
\"跨机房同步告警: Topic=$TOPIC Lag=$LAG > $ALERT_THRESHOLD\"}}"
    fi
  done

# 网络中断恢复策略
# 1. MM2 本地缓存: 源集群保留足够长的消息（retention.ms=7天）
# 2. 断网时生产端继续写 Beijing 集群
# 3. 恢复后 MM2 自动从 checkpoint offset 继续追赶
# 4. 监控恢复速率: (catchup_lag / time_elapsed) 评估完全 catch up 时间
```

| 维度 | MirrorMaker 2 | Cluster Linking (3.6+) | Confluent Replicator |
|---|---|---|---|
| **版本要求** | Kafka 2.4+ | Kafka 3.6+ | Confluent Platform 5.0+ |
| **部署方式** | 独立 Connect 进程 | Broker 内置，无需额外进程 | 独立 Connect 进程 |
| **资源开销** | 中等（独立 JVM） | 轻量（Broker 内联） | 较高（商业组件） |
| **Exactly-Once** | 不支持（At-Least-Once） | 支持（Broker 级同步） | 支持（商业版特性） |
| **双向同步** | 支持（前缀防回环） | 支持（Link 机制） | 支持 |
| **Offset 转换** | RemoteClusterUtils API | 内置自动转换 | 内置自动转换 |
| **ACL 同步** | 支持 | 支持 | 支持 |
| **监控集成** | JMX + Kafka Connect API | Broker Metrics | Confluent Control Center |
| **License** | Apache 2.0 开源 | Apache 2.0 开源 | 商业 License |
| **适用场景** | 开源首选，社区成熟 | 3.6+ 新架构，轻量部署 | 企业付费，全托管体验 |

## 一句话总结

**跨机房 Kafka 同步首选 MirrorMaker 2 实现单写多读架构，用 Topic 前缀（北京写入 → Shanghai 消费 beijing.* 前缀 Topic）天然防止双向回环，配合 Lag 监控和断网自动追赶策略，是生产环境最成熟可靠的方案。**


# Q95 日志收集系统

百万级日志/秒的收集架构采用三级流水线：Filebeat 采集层部署于各业务服务器 → Kafka 缓冲层按业务分 Topic（access/error/business），30 Partition + 3 副本 → Logstash/Flink 消费层分别清洗富化写入 ES（实时搜索）和 HDFS（离线分析），全程通过 Lag 监控告警保证管道健康。

## 日志收集三级流水线架构

<svg viewBox="0 0 720 595" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="q95-arrow-blue" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="#1565c0"/></marker>
    <marker id="q95-arrow-green" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="#3c8772"/></marker>
    <marker id="q95-arrow-orange" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="#e65100"/></marker>
    <marker id="q95-arrow-red" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="#e53935"/></marker>
    <marker id="q95-arrow-purple" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="#7b1fa2"/></marker>
    <marker id="q95-arrow-gray" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="#90a4ae"/></marker>
  </defs>

  <!-- 背景 -->
  <rect width="720" height="595" fill="#fafbfc" rx="8"/>

  <!-- ============================================ -->
  <!-- 顶部标题 -->
  <!-- ============================================ -->
  <rect x="100" y="8" width="520" height="26" rx="5" fill="#7b1fa2"/>
  <text x="360" y="26" text-anchor="middle" fill="#fff" font-size="13" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">百万级日志/秒收集架构 — 三级流水线</text>

  <!-- ============================================ -->
  <!-- 第一级：采集层 -->
  <!-- ============================================ -->
  <text x="360" y="52" text-anchor="middle" fill="#1565c0" font-size="12" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">第一级：采集层 (Filebeat x N)</text>

  <!-- App Server 1 -->
  <rect x="25" y="62" width="145" height="42" rx="5" fill="#1565c0" opacity="0.9"/>
  <text x="97" y="79" text-anchor="middle" fill="#fff" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">App Server 1</text>
  <text x="97" y="95" text-anchor="middle" fill="#bbdefb" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Filebeat · access.log</text>

  <!-- App Server 2 -->
  <rect x="185" y="62" width="145" height="42" rx="5" fill="#1565c0" opacity="0.9"/>
  <text x="257" y="79" text-anchor="middle" fill="#fff" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">App Server 2</text>
  <text x="257" y="95" text-anchor="middle" fill="#bbdefb" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Filebeat · access.log</text>

  <!-- App Server 3 -->
  <rect x="345" y="62" width="145" height="42" rx="5" fill="#1565c0" opacity="0.9"/>
  <text x="417" y="79" text-anchor="middle" fill="#fff" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">App Server 3</text>
  <text x="417" y="95" text-anchor="middle" fill="#bbdefb" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Filebeat · error.log</text>

  <!-- App Server N -->
  <rect x="505" y="62" width="145" height="42" rx="5" fill="#1565c0" opacity="0.9"/>
  <text x="577" y="79" text-anchor="middle" fill="#fff" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">App Server N</text>
  <text x="577" y="95" text-anchor="middle" fill="#bbdefb" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Filebeat · business.log</text>

  <!-- 右侧标注 -->
  <text x="665" y="78" text-anchor="middle" fill="#666" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">每台 1 个</text>
  <text x="665" y="93" text-anchor="middle" fill="#666" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Filebeat</text>

  <!-- 服务器到 Kafka 的箭头 -->
  <line x1="97" y1="104" x2="97" y2="130" stroke="#1565c0" stroke-width="1.6" marker-end="url(#q95-arrow-blue)"/>
  <line x1="257" y1="104" x2="257" y2="130" stroke="#1565c0" stroke-width="1.6" marker-end="url(#q95-arrow-blue)"/>
  <line x1="417" y1="104" x2="417" y2="130" stroke="#1565c0" stroke-width="1.6" marker-end="url(#q95-arrow-blue)"/>
  <line x1="577" y1="104" x2="577" y2="130" stroke="#1565c0" stroke-width="1.6" marker-end="url(#q95-arrow-blue)"/>

  <!-- ============================================ -->
  <!-- 第二级：Kafka 缓冲层 -->
  <!-- ============================================ -->
  <text x="360" y="148" text-anchor="middle" fill="#e65100" font-size="12" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">第二级：Kafka 缓冲层</text>

  <rect x="20" y="158" width="680" height="72" rx="8" fill="#fff" stroke="#333" stroke-width="1.5"/>

  <!-- Kafka 配置标签 -->
  <rect x="25" y="161" width="670" height="15" rx="3" fill="#fff3e0" stroke="#e65100" stroke-width="0.5"/>
  <text x="360" y="172" text-anchor="middle" fill="#e65100" font-size="8" font-family="PingFang SC, Microsoft YaHei, sans-serif">compression.type=lz4 | batch.size=64KB | linger.ms=10ms | retention.ms=7d | acks=1</text>

  <!-- Topic: access -->
  <rect x="30" y="179" width="200" height="44" rx="5" fill="#e3f2fd" stroke="#1565c0" stroke-width="1.5"/>
  <text x="130" y="196" text-anchor="middle" fill="#1565c0" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">Topic: access</text>
  <text x="130" y="213" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Partition x 30 · Replica = 3</text>

  <!-- Topic: error -->
  <rect x="250" y="179" width="200" height="44" rx="5" fill="#fce4ec" stroke="#e53935" stroke-width="1.5"/>
  <text x="350" y="196" text-anchor="middle" fill="#e53935" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">Topic: error</text>
  <text x="350" y="213" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Partition x 30 · Replica = 3</text>

  <!-- Topic: business -->
  <rect x="470" y="179" width="220" height="44" rx="5" fill="#fff3e0" stroke="#e65100" stroke-width="1.5"/>
  <text x="580" y="196" text-anchor="middle" fill="#e65100" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">Topic: business</text>
  <text x="580" y="213" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Partition x 30 · Replica = 3</text>

  <!-- Kafka 到消费层的箭头 -->
  <line x1="130" y1="230" x2="130" y2="268" stroke="#1565c0" stroke-width="1.6" marker-end="url(#q95-arrow-blue)"/>
  <line x1="350" y1="230" x2="350" y2="268" stroke="#e53935" stroke-width="1.6" marker-end="url(#q95-arrow-red)"/>
  <line x1="580" y1="230" x2="580" y2="268" stroke="#e65100" stroke-width="1.6" marker-end="url(#q95-arrow-orange)"/>

  <!-- ============================================ -->
  <!-- 第三级：消费层 -->
  <!-- ============================================ -->
  <text x="360" y="282" text-anchor="middle" fill="#3c8772" font-size="12" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">第三级：消费层 (Logstash / Flink)</text>

  <!-- Logstash 实时消费 -->
  <rect x="25" y="294" width="320" height="52" rx="7" fill="#e8f5e9" stroke="#3c8772" stroke-width="1.5"/>
  <text x="185" y="312" text-anchor="middle" fill="#3c8772" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">Logstash 实时消费</text>
  <text x="185" y="326" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Consumer Group: logstash-group · max.poll.records=500</text>
  <text x="185" y="340" text-anchor="middle" fill="#777" font-size="8" font-family="PingFang SC, Microsoft YaHei, sans-serif">清洗 / 过滤 / grok 字段解析 / geoip 富化</text>

  <!-- Flink 离线分析 -->
  <rect x="365" y="294" width="330" height="52" rx="7" fill="#ede7f6" stroke="#7b1fa2" stroke-width="1.5"/>
  <text x="530" y="312" text-anchor="middle" fill="#7b1fa2" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">Flink 离线分析</text>
  <text x="530" y="326" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Consumer Group: flink-group · Partition Discovery 30s</text>
  <text x="530" y="340" text-anchor="middle" fill="#777" font-size="8" font-family="PingFang SC, Microsoft YaHei, sans-serif">5min 滚动窗口聚合 · Checkpoint 精确一次</text>

  <!-- 消费层到存储的箭头 -->
  <line x1="185" y1="346" x2="185" y2="370" stroke="#3c8772" stroke-width="1.6" marker-end="url(#q95-arrow-green)"/>
  <line x1="530" y1="346" x2="530" y2="370" stroke="#7b1fa2" stroke-width="1.6" marker-end="url(#q95-arrow-purple)"/>

  <!-- Elasticsearch -->
  <rect x="55" y="373" width="260" height="34" rx="6" fill="#3c8772" opacity="0.88"/>
  <text x="185" y="392" text-anchor="middle" fill="#fff" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">Elasticsearch</text>
  <text x="185" y="403" text-anchor="middle" fill="#c8e6c9" font-size="8" font-family="PingFang SC, Microsoft YaHei, sans-serif">全文搜索 + Kibana 可视化</text>

  <!-- HDFS / 数据湖 -->
  <rect x="405" y="373" width="260" height="34" rx="6" fill="#7b1fa2" opacity="0.88"/>
  <text x="535" y="392" text-anchor="middle" fill="#fff" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">HDFS / 数据湖</text>
  <text x="535" y="403" text-anchor="middle" fill="#e1bee7" font-size="8" font-family="PingFang SC, Microsoft YaHei, sans-serif">Parquet 列存 + Hive/Spark 离线分析</text>

  <!-- ============================================ -->
  <!-- 监控告警层 -->
  <!-- ============================================ -->
  <line x1="20" y1="425" x2="700" y2="425" stroke="#e0e0e0" stroke-width="1.5" stroke-dasharray="6,4"/>
  <text x="360" y="448" text-anchor="middle" fill="#e53935" font-size="12" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">监控告警层</text>

  <!-- Consumer Lag -->
  <rect x="30" y="458" width="150" height="38" rx="5" fill="#fce4ec" stroke="#e53935" stroke-width="1"/>
  <text x="105" y="474" text-anchor="middle" fill="#e53935" font-size="10" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">Consumer Lag</text>
  <text x="105" y="490" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">阈值: &gt; 10 万告警</text>

  <!-- 吞吐量监控 -->
  <rect x="195" y="458" width="150" height="38" rx="5" fill="#e8f5e9" stroke="#3c8772" stroke-width="1"/>
  <text x="270" y="474" text-anchor="middle" fill="#3c8772" font-size="10" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">吞吐量监控</text>
  <text x="270" y="490" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">records/s per Topic</text>

  <!-- 磁盘使用率 -->
  <rect x="360" y="458" width="150" height="38" rx="5" fill="#e3f2fd" stroke="#1565c0" stroke-width="1"/>
  <text x="435" y="474" text-anchor="middle" fill="#1565c0" font-size="10" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">磁盘使用率</text>
  <text x="435" y="490" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">阈值: &gt; 80% 告警</text>

  <!-- Broker 健康 -->
  <rect x="525" y="458" width="150" height="38" rx="5" fill="#ede7f6" stroke="#7b1fa2" stroke-width="1"/>
  <text x="600" y="474" text-anchor="middle" fill="#7b1fa2" font-size="10" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">Broker 健康</text>
  <text x="600" y="490" text-anchor="middle" fill="#555" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">心跳 + ISR 检查</text>

  <!-- ============================================ -->
  <!-- Footer -->
  <!-- ============================================ -->
  <rect x="20" y="515" width="680" height="42" rx="6" fill="#f5f5f5" stroke="#e0e0e0" stroke-width="1"/>
  <text x="50" y="534" fill="#333" font-size="11" font-weight="bold" font-family="PingFang SC, Microsoft YaHei, sans-serif">三级流水线：采集 → 缓冲 → 消费</text>
  <text x="50" y="549" fill="#888" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">Filebeat / Kafka / ES + HDFS · 端到端延迟 &lt; 5s · 百万条/秒吞吐</text>
  <text x="650" y="542" text-anchor="middle" fill="#90a4ae" font-size="9" font-family="PingFang SC, Microsoft YaHei, sans-serif">百万条/秒</text>
</svg>

日志收集三级流水线是典型的生产者-消费者分离架构——Filebeat 作为轻量级采集器只负责"搬运"日志文件到 Kafka，Kafka 按业务维度拆分 Topic 并行缓冲，Logstash 和 Flink 各自以独立 Consumer Group 订阅消费，互不阻塞。整个管道容忍少量日志丢失（acks=1），优先保证吞吐量，并通过 lz4 压缩节省跨机房带宽。

## 端到端配置与代码实现

### Layer 1 — Filebeat 配置（采集日志发 Kafka）

```yaml
# filebeat.yml — 部署在每台业务服务器上
filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/app/access.log
    fields:
      topic: access
      server_id: ${SERVER_ID}
    multiline:
      pattern: '^\d{4}-\d{2}-\d{2}'
      negate: true
      match: after

  - type: log
    enabled: true
    paths:
      - /var/log/app/error.log
    fields:
      topic: error
      server_id: ${SERVER_ID}

  - type: log
    enabled: true
    paths:
      - /var/log/app/business.log
    fields:
      topic: business
      server_id: ${SERVER_ID}

# 输出到 Kafka
output.kafka:
  hosts: ["kafka-broker-1:9092", "kafka-broker-2:9092", "kafka-broker-3:9092"]
  topic: '%{[fields.topic]}'        # 按字段路由到不同 Topic
  partition.round_robin:
    reachable_only: true
  required_acks: 1                   # 容忍少量丢失，优先吞吐
  compression: lz4                   # 压缩节省带宽
  max_message_bytes: 1000000
  client_id: filebeat-${SERVER_ID}

# 日志处理
processors:
  - add_host_metadata: ~
  - add_cloud_metadata: ~
  - drop_fields:
      fields: ["agent.ephemeral_id"]
```

### Layer 2 — Kafka Topic 创建与分区规划（30 Partition, 3 副本, 7 天保留）

```bash
# 创建 3 个 Topic，每个 30 Partition, 3 副本, 7 天保留
kafka-topics.sh --bootstrap-server kafka-broker-1:9092 \
  --create --topic access \
  --partitions 30 --replication-factor 3 \
  --config retention.ms=604800000 \
  --config compression.type=lz4 \
  --config segment.bytes=1073741824 \
  --config max.message.bytes=1000000

kafka-topics.sh --bootstrap-server kafka-broker-1:9092 \
  --create --topic error \
  --partitions 30 --replication-factor 3 \
  --config retention.ms=604800000 \
  --config compression.type=lz4

kafka-topics.sh --bootstrap-server kafka-broker-1:9092 \
  --create --topic business \
  --partitions 30 --replication-factor 3 \
  --config retention.ms=604800000 \
  --config compression.type=lz4

# Producer 优化配置 (server.properties or topic-level)
# batch.size=65536            (64KB 批量发送)
# linger.ms=10                (最多等 10ms 凑批)
# compression.type=lz4        (lz4 在速度和压缩比之间最佳平衡)
# acks=1                      (Leader 确认即可，日志场景容忍少量丢失)
# buffer.memory=67108864      (64MB 发送缓冲区)
```

### Layer 3 — Logstash 消费 Kafka → ES（清洗 / 过滤 / 富化）

```
# logstash.conf — 消费 Kafka 三个 Topic，清洗后写入 ES
input {
  kafka {
    bootstrap_servers => "kafka-broker-1:9092,kafka-broker-2:9092,kafka-broker-3:9092"
    topics_pattern => "access|error|business"
    group_id => "logstash-group"
    consumer_threads => 10          # 30 Partition 配 10 线程并发消费
    auto_offset_reset => "latest"
    max_poll_records => 500
    decorate_events => true
    codec => "json"
  }
}

filter {
  # 解析 access 日志格式
  if [kafka][topic] == "access" {
    grok {
      match => {
        "message" => '%{IP:client_ip} - - \[%{HTTPDATE:timestamp}\] "%{WORD:method} %{URIPATHPARAM:request} HTTP/%{NUMBER:http_version}" %{NUMBER:status} %{NUMBER:bytes}'
      }
    }
    # 添加地理位置信息
    geoip { source => "client_ip" }
  }

  # error 日志不需要 grok，保持原始 message
  if [kafka][topic] == "error" {
    mutate {
      add_tag => ["error"]
      add_field => { "severity" => "ERROR" }
    }
  }

  # business 日志：JSON 解析 + 字段标准化
  if [kafka][topic] == "business" {
    json { source => "message" }
    mutate {
      rename => { "userId" => "user_id" }
      rename => { "orderAmount" => "order_amount" }
    }
  }

  # 移除不需要的字段
  mutate {
    remove_field => ["@version", "message", "[event][original]"]
  }
}

output {
  # 按 Topic 路由到不同 ES 索引
  if [kafka][topic] == "access" {
    elasticsearch {
      hosts => ["es-node-1:9200", "es-node-2:9200"]
      index => "access-logs-%{+YYYY.MM.dd}"
      template => "/etc/logstash/templates/access-template.json"
      template_name => "access-logs"
    }
  } else if [kafka][topic] == "error" {
    elasticsearch {
      hosts => ["es-node-1:9200", "es-node-2:9200"]
      index => "error-logs-%{+YYYY.MM.dd}"
    }
  } else {
    elasticsearch {
      hosts => ["es-node-1:9200", "es-node-2:9200"]
      index => "business-logs-%{+YYYY.MM.dd}"
    }
  }
}
```

### Layer 4 — Flink 消费 → HDFS（窗口聚合 + 离线分析）

```java
public class LogAnalysisJob {

    public static void main(String[] args) throws Exception {
        StreamExecutionEnvironment env =
            StreamExecutionEnvironment.getExecutionEnvironment();
        env.enableCheckpointing(60000); // 每分钟一次 Checkpoint

        // Kafka Source：独立 Consumer Group，不影响 Logstash
        KafkaSource<LogEvent> source = KafkaSource.<LogEvent>builder()
            .setBootstrapServers("kafka-broker-1:9092,kafka-broker-2:9092")
            .setTopics("access", "error", "business")
            .setGroupId("flink-group")
            .setStartingOffsets(OffsetsInitializer.latest())
            .setDeserializer(new LogEventDeserializer())
            // 自动发现新增 Partition
            .setProperty("partition.discovery.interval.ms", "30000")
            .build();

        DataStream<LogEvent> stream = env
            .fromSource(source, WatermarkStrategy.noWatermarks(), "Kafka Source");

        // 按 Topic 分流处理
        stream.process(new ProcessFunction<LogEvent, Object>() {
            @Override
            public void processElement(LogEvent event, Context ctx,
                                       Collector<Object> out) {
                switch (event.getTopic()) {
                    case "access":
                        // 按 URL 聚合 PV/UV（5 分钟滚动窗口）
                        stream.filter(e -> "access".equals(e.getTopic()))
                            .keyBy(LogEvent::getUrl)
                            .window(TumblingProcessingTimeWindows.of(Time.minutes(5)))
                            .aggregate(new PvUvAggregator())
                            .addSink(parquetSink("hdfs://namenode/logs/access_pv/"));
                        break;
                    case "error":
                        // 错误日志按类型统计
                        stream.filter(e -> "error".equals(e.getTopic()))
                            .keyBy(LogEvent::getErrorType)
                            .window(TumblingProcessingTimeWindows.of(Time.minutes(1)))
                            .aggregate(new ErrorCountAggregator())
                            .addSink(parquetSink("hdfs://namenode/logs/error_stats/"));
                        break;
                    case "business":
                        // 业务日志：订单量、GMV 等实时大盘
                        stream.filter(e -> "business".equals(e.getTopic()))
                            .keyBy(LogEvent::getEventType)
                            .window(TumblingProcessingTimeWindows.of(Time.minutes(1)))
                            .aggregate(new BusinessMetricsAggregator())
                            .addSink(parquetSink("hdfs://namenode/logs/business_metrics/"));
                        break;
                }
            }
        });

        env.execute("Log Analysis Pipeline");
    }

    // Parquet 列存 Sink（HDFS）
    private static FileSink<Object> parquetSink(String path) {
        return FileSink.forBulkFormat(new Path(path),
            ParquetAvroWriters.forReflectRecord(Object.class))
            .withRollingPolicy(
                OnCheckpointRollingPolicy.build())
            .build();
    }
}
```

## 日志收集方案对比

| 维度 | Filebeat + Kafka + ES | Fluentd + Kafka + HDFS |
|------|----------------------|------------------------|
| **采集器** | Filebeat (Go)，极轻量，CPU < 5%，内存 < 50MB | Fluentd (C/Ruby)，资源消耗稍高，插件丰富 |
| **缓冲层** | Kafka，磁盘持久化，多副本，积压能力几乎无限 | 同 Kafka |
| **消费层** | Logstash → ES，grok 解析能力强，适合实时搜索 | Flink → HDFS/Parquet，流批一体，适合离线分析 |
| **搜索能力** | ES + Kibana，全文搜索 + 可视化强 | HDFS 需 Hive/Spark/Impala 查询，延迟高 |
| **存储成本** | ES 索引占用磁盘大，按天 Rollover 需运维 | HDFS 三副本 + Parquet 列存压缩，成本低 |
| **实时性** | 端到端延迟 < 5 秒 | 分钟级窗口，准实时 |
| **数据丢失容忍** | acks=1 可能丢少量日志 | 同 Kafka acks 配置，Checkpoint 保证精确一次语义 |
| **监控** | ES 自带 + Kafka Lag Exporter | Flink Metrics + Kafka Lag Exporter |
| **适用场景** | 实时排障、全文本日志搜索、运维监控 | 离线日志分析、用户行为分析、数据仓库 ETL |

## 一句话总结

**百万级日志收集系统通过 Filebeat（轻量采集）→ Kafka（按业务分 Topic，30 Partition 并行 + lz4 压缩）→ Logstash/Flink（各自独立 Consumer Group 消费，同一份数据写入 ES 实时搜索和 HDFS 离线分析）三级流水线架构，以 acks=1 容忍少量丢失换取极致吞吐，用 Lag 监控全程保障管道健康。**

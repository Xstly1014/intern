(function(){const root=document.querySelector('#app');const data=window.INTERVIEW_DATA;let total=0;const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let html='';data.sections.forEach((section,si)=>{html+=`<section class="section"><h2>${si+1}. ${esc(section.title)}</h2><p class="section-note">${esc(section.note)}</p>`;section.questions.forEach((q,qi)=>{total++;const no=si*10+qi+1;html+=`<details class="question" data-search="${esc(q+' '+section.title)}"><summary>${no}. ${esc(q)}</summary><div class="answer"><p><strong>参考回答：</strong>${esc(section.answer)}</p><div class="tip"><strong>面试追问：</strong>${esc(section.followup)}</div></div></details>`});html+='</section>'});root.innerHTML=html;document.querySelector('#count').textContent=total;const input=document.querySelector('#search');input.addEventListener('input',()=>{const key=input.value.trim().toLowerCase();let visible=0;document.querySelectorAll('.question').forEach(x=>{const ok=!key||x.dataset.search.toLowerCase().includes(key);x.style.display=ok?'block':'none';if(ok)visible++});document.querySelector('#empty').style.display=visible?'none':'block'});})();

# 06 · 锁机制

::: tip 状态
目录框架已搭建，正文与图解编写中。
:::

## 计划内容

- [ ] 锁分类总览：按粒度（全局锁 / 表锁 / 行锁）、按模式（共享锁 / 排他锁）、按算法（记录 / 间隙 / 临键）
- [ ] 全局锁：FTWRL，全库逻辑备份场景
- [ ] 表级锁：表锁、元数据锁 MDL、意向锁 IS / IX
- [ ] 行级锁：记录锁 Record Lock、间隙锁 Gap Lock、临键锁 Next-Key Lock（图解加锁范围）
- [ ] 行锁加在索引上：无索引时行锁退化为表锁
- [ ] 当前读的加锁规则分析：等值查询、范围查询、唯一索引、非唯一索引
- [ ] 乐观锁 vs 悲观锁：版本号 / CAS 在业务层的实现
- [ ] 死锁：产生条件、InnoDB 死锁检测、等待超时
- [ ] 死锁排查：show engine innodb status、information_schema
- [ ] 如何避免死锁：固定顺序、缩短事务、合理索引
